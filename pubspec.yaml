name: florascanapp
description: "A new Flutter project."
publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ^3.11.4

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  flutter_blue_plus: ^2.1.2
  flutter_secure_storage: ^9.2.4
  supabase_flutter: ^2.0.2
  camera: ^0.11.0
  permission_handler: ^12.0.1
  google_ml_kit: ^0.14.0
  image_picker: ^1.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/images/FloraScan - Logo.png
    - assets/images/Cucumber Leaf.png
    - assets/images/Robusta Coffee Leaf.png
